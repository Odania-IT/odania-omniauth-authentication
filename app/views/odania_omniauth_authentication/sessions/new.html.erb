<p>
	<% OdaniaOmniauthAuthentication::Strategy.remaining_providers(current_user).each do |service| %>
		<%= link_to service, "/auth/#{service}/register" %>
	<% end %>
</p>

<p>
	<strong>Don&rsquo;t use these services?</strong>
	<%= link_to t('Create an account'), '/auth/identity/register' %> or
	<%= link_to t('Login'), '/auth/identity' %> with a password.
</p>
